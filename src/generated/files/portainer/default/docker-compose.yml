services:
  portainer:
    image: portainer/portainer-ce:sts
    restart: always
    expose:
      - 9000
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer-data:/data

volumes:
  portainer-data: {}
