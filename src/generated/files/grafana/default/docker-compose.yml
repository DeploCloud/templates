services:
  grafana:
    image: grafana/grafana-enterprise:13.2.2
    restart: unless-stopped
    expose:
      - 3000
    volumes:
      - grafana-storage:/var/lib/grafana

volumes:
  grafana-storage: {}
