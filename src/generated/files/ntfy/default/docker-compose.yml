services:
  ntfy:
    image: binwiederhier/ntfy:v2.28.0
    restart: unless-stopped
    expose:
      - 80
    command: serve
    volumes:
      - ntfy-cache:/var/cache/ntfy

volumes:
  ntfy-cache: {}
