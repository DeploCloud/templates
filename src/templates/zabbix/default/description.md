Recommended Zabbix deployment following the official default Compose setup: Zabbix server, Nginx web interface and MySQL in separate containers. The database persists in a volume. Choose the PostgreSQL variant if you prefer PostgreSQL.
