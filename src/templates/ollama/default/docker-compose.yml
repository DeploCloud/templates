services:
  ollama:
    image: ollama/ollama:0.33.3
    restart: unless-stopped
    expose:
      - 11434
    environment:
      OLLAMA_HOST: ${OLLAMA_HOST}
    volumes:
      - ollama-data:/root/.ollama

volumes:
  ollama-data: {}
