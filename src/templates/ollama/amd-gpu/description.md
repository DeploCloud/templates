Ollama local model server using AMD GPU acceleration. This variant requires a Linux host with a supported AMD GPU, ROCm and access to `/dev/kfd` and `/dev/dri`; downloaded models persist in a volume.
