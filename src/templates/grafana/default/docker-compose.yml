services:
  grafana:
    image: grafana/grafana-enterprise:12.4.9
    restart: unless-stopped
    expose:
      - 3000
    volumes:
      - grafana-storage:/var/lib/grafana

volumes:
  grafana-storage: {}
