services:
  freshrss:
    image: freshrss/freshrss:1.29.1
    restart: unless-stopped
    expose:
      - 80
    environment:
      TZ: ${TZ}
    volumes:
      - freshrss-data:/var/www/FreshRSS/data

volumes:
  freshrss-data: {}
