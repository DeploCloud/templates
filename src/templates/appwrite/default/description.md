Appwrite's official self-hosted Compose stack with Traefik, the API, console, workers, PostgreSQL,
MongoDB, Redis, ClickHouse and the function runtime. Application data and uploaded files persist in
named volumes.
