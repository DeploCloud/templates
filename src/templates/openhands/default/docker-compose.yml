services:
  openhands:
    image: ghcr.io/all-hands-ai/openhands:0.9.1
    restart: unless-stopped
    expose:
      - 3000
    volumes:
      - openhands-data:/openhands

volumes:
  openhands-data: {}
