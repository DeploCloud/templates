SeaweedFS is a distributed file and object store. The official mini mode starts a compact single-node deployment with an S3 endpoint, master UI and Filer UI, protected by generated S3 credentials.

Add more volume servers when you need a multi-node layout; this template is intentionally a straightforward starting point.
