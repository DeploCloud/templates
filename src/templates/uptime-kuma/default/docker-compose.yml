services:
  uptime-kuma:
    image: louislam/uptime-kuma:2.5.4
    restart: always
    expose:
      - 3001
    volumes:
      - uptime-kuma-data:/app/data
      - /var/run/docker.sock:/var/run/docker.sock

volumes:
  uptime-kuma-data:
