services:
  radarr:
    image: lscr.io/linuxserver/radarr:latest
    restart: unless-stopped
    expose:
      - 7878
    environment:
      PUID: ${PUID}
      PGID: ${PGID}
      TZ: ${TZ}
    volumes:
      - radarr-config:/config

volumes:
  radarr-config: {}
