services:
  calibre-web:
    image: lscr.io/linuxserver/calibre-web:version-v5.7.2
    restart: unless-stopped
    expose:
      - 8083
    environment:
      PUID: ${PUID}
      PGID: ${PGID}
      TZ: ${TZ}
    volumes:
      - calibre-web-config:/config

volumes:
  calibre-web-config: {}
